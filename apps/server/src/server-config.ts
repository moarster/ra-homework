/**
 * Числовые правила самого сервера: то, что не является контрактом системы и не нужно
 * фронтенду. Все, что нужно обеим сторонам, живет в `packages/contracts/src/config.ts`.
 */

export const SERVER_RULES = {
  /** Через столько секунд без данных статус машины становится NO_DATA, а светофор - серым. */
  noDataStatusSeconds: 60,
  /** Не чаще этого числа тиков websocket в секунду реального времени (раздел 8 `SPEC.md`). */
  tickRateHz: 4,
  /** Не более этого числа точек позиции на машину в одном тике. */
  maxTickPositions: 60,
  /** Порог буфера сокета, выше которого тик пропускается: медленный клиент не копит очередь. */
  socketBufferLimitBytes: 512 * 1024,
  /** Период пинга websocket, миллисекунды. */
  pingIntervalMs: 15_000,
  /** Как часто чистить журнал событий, виртуальные секунды. */
  eventPruneIntervalSeconds: 600,
} as const;
